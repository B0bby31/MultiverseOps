---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./external-secret.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: hass-config
    files:
      - configuration.yaml=./hass-config.yaml
configurations:
  - kustomizeconfig.yaml
