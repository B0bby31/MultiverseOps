This folder is for clusterwide secrets! For instance domains or things that occur frequently.

Once bitwarden secrets supports it, I will add the docker auth credentials as a cluster wide secret here.